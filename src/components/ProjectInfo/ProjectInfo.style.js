import styled from "styled-components";

const StyledProjectInfo = styled.div`
  position: absolute;
  bottom: 5%;
  right: 2%;

  p {
    font-size: 19px;
  }

  a {
    color: #1f30cb;
    margin-left: 5px;
  }
`;

export default StyledProjectInfo;
